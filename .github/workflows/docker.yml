name: Build & Push Docker Images

on:
  push:
    branches: [ develop ]
  workflow_dispatch:

concurrency:
  group: docker-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Backend
        run: |
          docker build -f backend/Dockerfile -t yerriiniii/inventory-backend:latest .
          docker push yerriiniii/inventory-backend:latest

      - name: Build & Push Frontend
        run: |
          docker build -f frontend/Dockerfile -t yerriiniii/inventory-frontend:latest ./frontend
          docker push yerriiniii/inventory-frontend:latest